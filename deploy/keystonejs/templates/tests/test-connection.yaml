apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keystonejs.fullname" . }}-test-connection"
  labels:
    {{- include "keystonejs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keystonejs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
